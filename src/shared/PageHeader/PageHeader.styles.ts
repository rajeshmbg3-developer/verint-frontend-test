import { makeStyles } from "@mui/styles";

const usePageHeaderStyle = makeStyles(() => ({
  logoContainer: {
    height: "2.5rem",
  },
  flex: { display: "flex" },
}));

export default usePageHeaderStyle;
